import { Meta } from '@storybook/addon-docs'
import theme from './'

<Meta title="Styleguide/Palette" />

<style>{`
    .color-preview {
        text-align: right;
        padding: 16px;
        box-shadow: 0 1px 4px rgba(0,0,0,0.5);
        margin: 8px;
    }

    .value-picker {
        margin: 8px;
        border: 0;
        border-radius: 0;
        resize: none;
        padding: 4px 8px;
        color: #ffffff;
        background: rgba(0,0,0,0.5);
    }
`}</style>

# Palette

<>
  {Object.keys(theme.palette).map((name) => (
    <div key={name}>
      <h2>{name}</h2>
      <div
        className="color-preview"
        style={{ background: theme.palette[name].hex() }}
      >
        <textarea
          defaultValue={theme.palette[name].hex()}
          className="value-picker"
          rows="1"
        ></textarea>
        <textarea
          defaultValue={theme.palette[name].rgb()}
          className="value-picker"
          rows="1"
        ></textarea>
        <textarea
          defaultValue={theme.palette[name].hsl()}
          className="value-picker"
          rows="1"
        ></textarea>
      </div>
    </div>
  ))}
</>
