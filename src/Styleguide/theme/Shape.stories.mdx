import { Meta } from '@storybook/addon-docs'
import theme from './'

<Meta title="Styleguide/Shape" />

<style>{`
    .border-radius-preview {
        border-radius: ${theme.shape.borderRadius.rem()};
        width: 100px;
        height: 100px;
        background: #000;
        margin: 1rem;
    }
`}</style>

# Shape

## Border Radius

<p>
  Global Border radius is {theme.shape.borderRadius.rem()} or{' '}
  {theme.shape.borderRadius.px()}
</p>

<div className="border-radius-preview" />
