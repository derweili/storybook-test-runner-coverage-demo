import { Meta } from '@storybook/addon-docs'
import theme from './'
import { Easing, Duration } from '../types'
import { SpacingPreview } from '../../../docs/preview'

<Meta title="Styleguide/Transition" />

# Transition

## Easing

<ul>
  {Object.keys(Easing).map((name) => (
    <li key={name}>{name}</li>
  ))}
</ul>

## Duration

<ul>
  {Object.keys(Duration).map((name) => (
    <li key={name}>{name}</li>
  ))}
</ul>
