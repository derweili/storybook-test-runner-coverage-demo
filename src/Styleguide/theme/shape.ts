import { SizeValue } from '../value'
import { Shape } from './types'

const shape: Shape = {
  // atm it would be 5px, but Borstie suggested a 3px value
  borderRadius: new SizeValue(3),
  progressSickness: 3.6,
}

export default shape
